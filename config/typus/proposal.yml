# Use the README file as a reference to customize settings.

Proposal:
  fields:
    default: title, description, large_description, image_url, category
    form: title, description, large_description, image_url, category
  relationships: category
  application: Application
